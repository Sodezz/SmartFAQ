Сервис: Умный FAQ
Стек: Docker/Docker-compose, FastAPI, PostgreSQL, Starlette Admin.
Задачи:
1) Создать сервис на FastAPI с админкой Starlette Admin и подключение к бд PostgreSQL
2) В админке создать раздел с загружаемыми документами.
3) После загрузки документа, он отправляется на векторизацию в внешний сервис(позже придумаем в какой). Результат векторизации должен сохраниться в бд.
4) Создать API которое будет принимать в себя запрос пользователя в формате свободной строки, производить векторизацию и выдавать ответ из документов в соответствии с запросом пользователя

короче посоветую с чего начать: 
начни с того чтобы просто развернуть fast api, но это ты вроде сделал. 
потом заверни его в докер. 
А лучше в docker-compose, потому что у тебя там еще и бд нужна. 
Можешь поискать примеры docker-compose файлов с сервисом и бд. 
Через композер ими проще управлять